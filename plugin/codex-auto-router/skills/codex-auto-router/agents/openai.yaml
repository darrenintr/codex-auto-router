interface:
  display_name: "Codex Auto Router"
  short_description: "Route pending local Codex tasks through ChatGPT."
  default_prompt: "Use $codex-auto-router to find my pending local Codex request, choose the cheapest sufficient tier, and submit the route so the waiting Codex launcher can resume."
